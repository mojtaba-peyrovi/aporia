# Interview Practice Coach

## Local run

```bash
uv sync
uv run streamlit run interview_coach/app.py
```

## Tests

```bash
uv run pytest -q
```

## Secrets

- Set `OPENAI_API_KEY` via Streamlit secrets or environment variables.

