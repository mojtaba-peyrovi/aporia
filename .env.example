# OpenAI
OPENAI_API_KEY=your_openai_api_key

# Optional: use MySQL instead of SQLite fallback.
# When unset, the app uses local SQLite files under `interview_coach/.data/`.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=aporia
MYSQL_USER=aporia
MYSQL_PASSWORD=aporia
